export type NavItem = {
  name: string;
  path: string;
};

export type Category = {
  label: string;
  value: string;
  icon?: string;
};

export type SuggestionItem = {
  _id?: string;
  title?: string;
  name?: string;
};